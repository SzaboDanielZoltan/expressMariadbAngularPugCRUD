extends layout

block content
  .container
    .row
      .col-4.offset-4
        form(action="/titles/create" method="post")
          .form-group
            label(for="title") Title name
            input.form-control(name="title" id="title" type="text")
          button.btn.btn-primary Send
          a.btn.btn-danger(href="/titles") Cancel